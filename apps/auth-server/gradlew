#!/usr/bin/env sh
# Minimal wrapper script that delegates to the repository-level Gradle wrapper.
set -e
DIR="$(cd "$(dirname "$0")" && pwd)"
REPO_ROOT="$(cd "$DIR/../.." && pwd)"
exec "$REPO_ROOT/gradlew" -p "$DIR" "$@"
